<%= form_with(model: upload, data: {controller: "file-upload-link"}) do |form| %>
  <% if upload.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(upload.errors.count, "error") %> prohibited this upload from being saved:</h2>

      <ul>
        <% upload.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :file %>
    <%= form.file_field :file, data: {controller: 'autoclick', file_upload_link_target: 'filePicker', action: 'file-upload-link#submit'} %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
