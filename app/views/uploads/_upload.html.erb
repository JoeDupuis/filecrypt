<div id="<%= dom_id upload %>">
  <%= upload.file.filename %> -   <%= upload.created_at %>

  <% if upload.file.representable? %>
    <%= image_tag upload.file.representation(resize_to_limit: [100, 100]) %>
  <% end %>
</div>
