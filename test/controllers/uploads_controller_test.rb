require "test_helper"

class UploadsControllerTest < ActionDispatch::IntegrationTest
  setup do
    @upload = uploads(:one)
  end

  test "uploads are per users" do
    skip
  end

  test "uploads copy link are disposition inline" do
    skip
  end

  test "uploads show view links are disposition inline" do
    skip
  end
end
